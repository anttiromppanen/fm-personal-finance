/**
 * @description Color variables for components that need dynamic color by index.
 * All color variables need to be in sync with each other (indexes contain the same color).
 */

export const tailwindBorderColors = [
  "border-l-secondaryGreen",
  "border-l-secondaryTeal",
  "border-l-primaryDarkGrey",
  "border-l-secondaryBrown",
  "border-l-primaryDark",
  "border-l-primaryLightGrey",
];

/**
 * @description Color variables for components that need dynamic color by index.
 * All color variables need to be in sync with each other (indexes contain the same color).
 */

export const hexBgColors = [
  "#277C77",
  "#81C9D7",
  "#625F70",
  "#F1CDAB",
  "#201F24",
  "#B4B3B8",
];
